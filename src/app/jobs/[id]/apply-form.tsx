"use client";

import { useState } from "react";

export function ApplyForm({ jobId }: { jobId: string }) {
  const [availability, setAvailability] = useState(true); const [rate, setRate] = useState(0); const [message, setMessage] = useState(""); const [experience, setExperience] = useState(""); const [state, setState] = useState(""); const [saving, setSaving] = useState(false);
  async function submit(event: React.FormEvent) { event.preventDefault(); setSaving(true); setState(""); const response = await fetch(`/api/applications?jobId=${jobId}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ availability, proposedRateCents: Math.round(rate * 100), message, relevantExperience: experience }) }); const result = await response.json(); setSaving(false); setState(response.ok ? "Je reactie is verstuurd." : result.error ?? "Reageren is niet gelukt."); if (response.ok) { setMessage(""); setExperience(""); } }
  return <form onSubmit={submit} className="mt-10 border-t border-slate-100 pt-8"><h2 className="text-xl font-bold text-slate-950">Reageren op opdracht</h2><p className="mt-2 text-sm text-slate-600">Log in als SECURITY_PROFESSIONAL om je reactie te versturen.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="flex items-center gap-3 text-sm font-semibold"><input type="checkbox" checked={availability} onChange={(event) => setAvailability(event.target.checked)} /> Ik ben beschikbaar</label><label className="field">Gewenst uurtarief<input required type="number" min="0" step="0.50" value={rate} onChange={(event) => setRate(Number(event.target.value))} /></label><label className="field sm:col-span-2">Bericht<textarea required minLength={10} rows={4} value={message} onChange={(event) => setMessage(event.target.value)} /></label><label className="field sm:col-span-2">Relevante ervaring<textarea required minLength={10} rows={4} value={experience} onChange={(event) => setExperience(event.target.value)} /></label></div>{state && <p role="status" className="mt-4 rounded-lg bg-slate-100 p-3 text-sm text-slate-700">{state}</p>}<button disabled={saving} className="mt-5 min-h-11 rounded-lg bg-orange-500 px-5 text-sm font-bold text-white disabled:opacity-60">{saving ? "Versturen..." : "Reageren op opdracht"}</button></form>;
}